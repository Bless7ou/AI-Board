# ChemBoard - 전자칠판 연동 화학 시뮬레이션 시스템

## 프로젝트 개요

**프로젝트명:** ChemBoard  
**분류:** 교육 EdTech / 인터랙티브 학습 도구  
**대상:** 중·고등학교 화학 수업 (교사 및 학생)  
**테스트 환경:** iPad (전자칠판 대체)

---

## 문제 정의 (Pain Point)

화학은 원자·분자 수준의 추상적 개념을 다루기 때문에 시각화가 매우 중요하다.  
하지만 현재 교실 환경에서는 다음과 같은 문제가 있다.

- 교사가 칠판에 화학식·반응식을 판서해도 학생은 **정적인 그림**만 본다
- 원자 간 결합, 전자 이동, 반응 메커니즘 등 **동적 과정**을 이해하기 어렵다
- 별도 시뮬레이션 툴을 실행하려면 수업 흐름이 끊긴다
- 교사가 직접 입력하는 **판서 흐름에 맞춘 실시간 시각화**가 없다

---

## 솔루션

> 교사가 전자칠판(또는 iPad)에 화학 관련 내용을 판서하면,  
> 시스템이 이를 인식하고 **자동으로 대응하는 애니메이션 시뮬레이션**을 생성한다.

---

## 핵심 기능

### 1. 판서 인식 (Input Recognition)
- **인식 트리거 1:** 교사가 **[전체 인식] 버튼을 터치하는 순간** Canvas 전체를 캡처하여 한 번만 인식
- **인식 트리거 2:** **[부분 인식] 버튼** 후 드래그로 영역을 선택하면 해당 영역만 캡처하여 인식
- 화학식 텍스트 인식: `H₂O`, `NaCl`, `CO₂` 등
- 화학 반응식 인식: `2H₂ + O₂ → 2H₂O`
- 키워드 인식: `이온결합`, `공유결합`, `산화환원`, `산염기`, `극성`, `무극성`, `연소` 등
- **Google Cloud Vision API** 기반 OCR + 화학 특화 파서 적용
- OCR 오인식 자동 보정: `H20→H2O`, `C02→CO2`, `HCI→HCl` 등
- 수동 입력 모드 지원 (OCR 없이 직접 텍스트 입력)

### 2. 시뮬레이션 자동 생성
판서 내용에 따라 아래 시뮬레이션 중 적절한 것을 자동 선택·실행한다.

| 판서 내용 | 생성되는 시뮬레이션 | 구현 상태 |
|---|---|---|
| 화학식 (예: H₂O, NaCl, CO₂) | PubChem 2D 분자 구조 + 결합 시각화 + 회전 애니메이션 | ✅ 구현 |
| 반응식 (예: 2H₂ + O₂ → 2H₂O) | 반응물 → 반응 진행 → 생성물 단계별 애니메이션 | ✅ 구현 |
| 연소 반응 | 불꽃 애니메이션 + 반응 전/후 분자 시각화 | ✅ 구현 |
| 중화 반응 | HCl + NaOH → NaCl + H₂O 이온 반응 표시 | ✅ 구현 |
| 이온결합 (예: NaCl) | 3단계 전자 이동 및 이온 형성 과정 애니메이션 | ✅ 구현 |
| 공유결합 (예: H₂O) | 전자쌍 공유 과정 + H₂O 굽은형 구조(104.5°) | ✅ 구현 |
| 산염기 중화 | H⁺/OH⁻ 이온 결합 과정 | ✅ 구현 |
| 원소 단독 (예: Na, Fe) | 해당 원소 전자 배치 시각화 (궤도 애니메이션) | ✅ 구현 |
| 산화환원 반응 | 전자 이동 방향 설명 텍스트 표시 | ⚠️ 부분구현 (애니메이션 미완성) |

### 3. 인터랙티브 제어
- 교사: 시뮬레이션 속도 조절 (0.25x ~ 3x), 일시정지/재생
- 시뮬레이션 패널과 수업 보조 패널은 **드래그로 자유롭게 이동** 가능
- 색상 선택 (흰색/노랑/빨강/초록/파랑), 펜 크기 조절 (1~12)
- 펜 / 지우개 도구 전환

---

## 기술 스택

### 프론트엔드 (iPad 최적화 웹앱)
- **React** + **TypeScript**
- **Vite** - 빌드 툴
- **Canvas 2D API** - 분자 구조 렌더링 및 모든 시뮬레이션 애니메이션

### 판서 인식
- **Canvas API** - 필기 입력 캡처 (마우스 + 터치 이벤트 모두 지원)
- **Google Cloud Vision API** - 클라우드 기반 OCR (손글씨 → 텍스트, 월 1,000회 무료)
- **자체 화학식 파서** - 인식된 텍스트 → 화학 데이터 구조 변환 + OCR 오류 자동 보정

### 화학 데이터
- **PubChem REST API** - 분자 2D 구조·결합 데이터 실시간 조회 (메모리 캐시 적용)
- **자체 원소 데이터베이스** (`atomData.ts`) - 원소 색상, 반지름, 전기음성도, 원자가 전자
- **자체 반응 키워드 사전** (`moleculeData.ts`) - 한국어 키워드 → 시뮬레이션 타입 매핑

---

## 지원 화학 단원 (중·고 교육과정 기준)

### 중학교
- 물질의 구성 (원자, 분자, 이온)
- 화학식과 화학 반응식
- 산·염기·중화 반응

### 고등학교
- 화학 결합 (이온결합, 공유결합)
- 분자 구조와 극성 (H₂O 굽은형 104.5°, 비공유전자쌍)
- 산화환원 반응 (부분 구현)
- 연소 반응

---

## 시스템 흐름

```
[교사 판서 (자유롭게 작성)]
    ↓
[교사가 [전체 인식] 또는 [부분 인식] 버튼 터치]
    ↓
[Canvas 이미지 캡처 (전체 또는 선택 영역)]
    ↓
[Google Cloud Vision API로 OCR]
    ↓
[OCR 오류 보정 + 화학식 파서로 텍스트 분류]
    ↓
[화학 키워드/수식 타입 결정]
    ↓  ↓  ↓  ↓  ↓
[분자] [반응식] [이온결합] [공유결합] [전자배치] [산염기] ...
    ↓
[Canvas 2D 애니메이션 렌더링 (PubChem 데이터 활용)]
    ↓
[플로팅 시뮬레이션 패널에 실시간 표시]
    ↓
[수업 보조 패널에 인식 내용 자동 누적]
```

---

## 화면 구성 (UI Layout)

```
┌─────────────────────────────────────────────────────────┐
│  ⚗ ChemBoard   AI 화학 수업 어시스턴트     ✏️ ⚙️         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   판서 영역 (전체 Canvas)          ┌──────────────────┐  │
│                                   │  🔬 시뮬레이션    │  │
│   교사가 여기에 화학식/키워드 작성   │  (드래그 이동 가능)│  │
│                                   └──────────────────┘  │
│                      [인식 결과 배지]                    │
│                                   ┌──────────────────┐  │
│                                   │  🤖 수업 보조     │  │
│                                   │  (드래그 이동 가능)│  │
│                                   └──────────────────┘  │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ 🖊 🧹 | ● ● ● ● ● | [크기] | 🔍전체인식 ✂️부분인식     │
│                            | ⏸/▶ | 속도 ◀ 0.25x ▶     │
│                            | 🗑초기화 | 📋요약           │
└─────────────────────────────────────────────────────────┘
```

---

## 개발 단계 (MVP → 완성)

### Phase 1 - MVP (핵심 기능) ✅ 완료
- [x] 판서 캔버스 구현 (iPad 터치 입력, 마우스 입력)
- [x] 화학식 텍스트 입력 → PubChem 분자 구조 시각화
- [x] 주요 시뮬레이션 구현 (이온결합, 공유결합, 반응식, 전자배치, 산염기)
- [x] Google Cloud Vision API OCR 통합
- [x] 부분 영역 선택 OCR
- [x] iPad Safari에서 동작 확인

### Phase 2 - 기능 확장
- [x] OCR 기반 손글씨 인식 통합 (Google Vision)
- [x] 반응 애니메이션 다양화 (연소, 중화, 일반 반응)
- [ ] 산화환원 반응 애니메이션 구현 (현재 텍스트 설명만 표시)
- [ ] 한국어 화학 키워드 확장 (동적 평형, 금속결합, 전기화학 등)
- [ ] 교사/학생 역할 분리 (화면 공유 모드)

### Phase 3 - 완성
- [ ] 전자칠판 연동 (미러링 또는 앱 연결)
- [ ] 학생 인터랙션 기능 (질문, 퀴즈)
- [ ] PWA / 오프라인 지원 (Service Worker)
- [ ] 멀티터치 지원 (핀치-줌)

---

## 수업 보조 기능 (구현됨)

> ChemBoard는 단순한 시뮬레이션 도구를 넘어, **수업 흐름을 자동으로 기록·정리하는 보조 시스템**으로 동작한다.

### 실시간 수업 내용 자동 정리

#### 1. 인식 내용 타임라인
- 인식할 때마다 시간(HH:MM) + 유형 태그 + 화학식을 **수업 보조 패널에 자동 누적**
- 유형별 색상 태그: 분자(파랑), 반응식(주황), 이온결합(보라), 공유결합(민트), 전자배치(노랑), 산염기(분홍)

#### 2. 오늘의 요약 자동 생성
- 다룬 화학식, 반응식, 개념을 **실시간으로 자동 분류·집계**
- 수업 보조 패널 하단에 항상 표시

#### 3. 수업 요약 모달 (📋 요약 버튼)
- 수업이 끝나면 **[요약] 버튼**으로 수업 전체 요약을 한 화면에 확인
- 다룬 화학식 / 반응식 / 개념 목록 + 수업 흐름 타임라인
- **클립보드 복사** 기능으로 즉시 학생 복습 자료로 활용

### 컨셉: 수업 자동 기록 시스템

```
[교사가 수업 진행]
    ↓
칠판에 판서 → [인식] 버튼 터치
    ↓
화학식/키워드 → 시뮬레이션 자동 실행
    ↓
수업 보조 패널에 인식 내용 실시간 누적
    ↓
수업 종료 → [요약] 버튼 → 수업 전체 요약 확인 + 복사
```

- 교사는 수업에만 집중하고, **시스템이 시각화·기록·요약을 자동 처리**
- 학생은 판서 받아쓰기 대신 **수업 내용 이해에 집중**
- 수업이 끝나면 복습 자료가 자동으로 완성

---

## 기대 효과

- 판서와 시뮬레이션이 **동시에 이루어져** 수업 흐름이 끊기지 않는다
- 추상적인 화학 개념을 **시각적·동적으로** 즉시 확인할 수 있다
- 별도 앱 설치 없이 **웹 브라우저(iPad Safari)** 만으로 동작한다
- 교사의 **판서 습관을 바꾸지 않고** 기존 수업 방식에 자연스럽게 통합된다
- 수업 보조 시스템이 **인식 내용 기록·수업 요약**을 자동화하여 교사의 부담을 줄인다
