# ChemBoard - 전자칠판 연동 화학 시뮬레이션 시스템

## 프로젝트 개요

**프로젝트명:** ChemBoard  
**분류:** 교육 EdTech / 인터랙티브 학습 도구  
**대상:** 중·고등학교 화학 수업 (교사 및 학생)  
**테스트 환경:** iPad (전자칠판 대체)

---

## 문제 정의 (Pain Point)

화학은 원자·분자 수준의 추상적 개념을 다루기 때문에 시각화가 매우 중요하다.  
하지만 현재 교실 환경에서는 다음과 같은 문제가 있다.

- 교사가 칠판에 화학식·반응식을 판서해도 학생은 **정적인 그림**만 본다
- 원자 간 결합, 전자 이동, 반응 메커니즘 등 **동적 과정**을 이해하기 어렵다
- 별도 시뮬레이션 툴을 실행하려면 수업 흐름이 끊긴다
- 교사가 직접 입력하는 **판서 흐름에 맞춘 실시간 시각화**가 없다

---

## 솔루션

> 교사가 전자칠판(또는 iPad)에 화학 관련 내용을 판서하면,  
> 시스템이 이를 인식하고 **자동으로 대응하는 애니메이션 시뮬레이션**을 생성한다.

---

## 핵심 기능

### 1. 판서 인식 (Input Recognition)
- **인식 트리거:** 매 프레임이 아닌, 교사가 **[인식] 버튼을 터치하는 순간** Canvas 전체를 캡처하여 한 번만 인식
- 화학식 텍스트 인식: `H₂O`, `NaCl`, `CO₂` 등
- 화학 반응식 인식: `2H₂ + O₂ → 2H₂O`
- 키워드 인식: `이온결합`, `공유결합`, `산화환원`, `산염기` 등
- 손글씨 OCR + 화학 특화 파서 적용

### 2. 시뮬레이션 자동 생성
판서 내용에 따라 아래 시뮬레이션 중 적절한 것을 자동 선택·실행한다.

| 판서 내용 | 생성되는 시뮬레이션 |
|---|---|
| 화학식 (예: H₂O) | 분자 3D 구조 + 결합 시각화 |
| 반응식 (예: 2H₂ + O₂ → 2H₂O) | 반응 전/후 원자 재배열 애니메이션 |
| 이온결합 (예: NaCl) | 전자 이동 및 이온 형성 과정 |
| 공유결합 (예: N₂) | 전자쌍 공유 과정 애니메이션 |
| 산화환원 반응 | 전자 이동 방향 화살표 애니메이션 |
| 산염기 중화 | H⁺/OH⁻ 이온 결합 과정 |
| 원소 주기율 언급 | 해당 원소 전자 배치 시각화 |

### 3. 인터랙티브 제어
- 교사: 시뮬레이션 속도 조절, 일시정지, 단계별 진행
- 학생(iPad): 분자 모델 회전·확대 가능 (터치 지원)
- 시뮬레이션 화면과 칠판 화면 동시 표시 (분할 화면)

---

## 기술 스택

### 프론트엔드 (iPad 최적화 웹앱)
- **React** + **TypeScript**
- **Three.js** / **Babylon.js** - 3D 분자 모델 렌더링
- **Framer Motion** - 2D 반응 애니메이션
- PWA (Progressive Web App) - iPad 홈 화면 추가 지원

### 판서 인식
- **Canvas API** - 필기 입력 캡처
- **Tesseract.js** - 브라우저 내 OCR (손글씨 → 텍스트)
- **자체 화학식 파서** - 인식된 텍스트 → 화학 데이터 구조 변환

### 화학 데이터
- **PubChem API** - 분자 구조 데이터 조회
- **자체 반응 데이터베이스** - 중·고 교육과정 주요 반응 수록

---

## 지원 화학 단원 (중·고 교육과정 기준)

### 중학교
- 물질의 구성 (원자, 분자, 이온)
- 화학식과 화학 반응식
- 산·염기·중화 반응

### 고등학교
- 화학 결합 (이온결합, 공유결합, 금속결합)
- 분자 구조와 극성
- 산화환원 반응
- 동적 평형과 반응 속도
- 전기화학 (전지, 전기분해)

---

## 시스템 흐름

```
[교사 판서 (자유롭게 작성)]
    ↓
[교사가 [인식] 버튼 터치]
    ↓
[Canvas 전체 이미지 캡처 (1회)]
    ↓
[OCR + 화학식 파서로 텍스트 인식]
    ↓
[화학 키워드/수식 분류]
    ↓
[해당 시뮬레이션 모듈 호출]
    ↓
[Three.js / Canvas 애니메이션 렌더링]
    ↓
[iPad 화면에 실시간 표시]
```

---

## 화면 구성 (UI Layout)

```
┌─────────────────────────────────────────────┐
│  ChemBoard                          [설정] [?] │
├────────────────────┬────────────────────────┤
│                    │                        │
│   판서 영역         │   시뮬레이션 영역        │
│   (Canvas)         │   (3D / 애니메이션)      │
│                    │                        │
│   교사가 여기에     │   인식된 내용에 맞는     │
│   화학식/키워드     │   시뮬레이션 자동 실행   │
│   작성              │                        │
│                    │                        │
├────────────────────┴────────────────────────┤
│  [인식] [재생] [일시정지] [단계별] [속도 ◀ ▶] [초기화] │
└─────────────────────────────────────────────┘
```

---

## 개발 단계 (MVP → 완성)

### Phase 1 - MVP (핵심 기능)
- [ ] 판서 캔버스 구현 (iPad 터치 입력)
- [ ] 화학식 텍스트 입력 → 분자 구조 시각화
- [ ] 주요 반응식 3~5개 시뮬레이션 구현
- [ ] iPad Safari에서 동작 확인

### Phase 2 - 기능 확장
- [ ] OCR 기반 손글씨 인식 통합
- [ ] 반응 애니메이션 다양화
- [ ] 교사/학생 역할 분리 (화면 공유 모드)

### Phase 3 - 완성
- [ ] 전자칠판 연동 (미러링 또는 앱 연결)
- [ ] 학생 인터랙션 기능 (질문, 퀴즈)
- [ ] 오프라인 지원 (Service Worker)

---

## 기대 효과

- 판서와 시뮬레이션이 **동시에 이루어져** 수업 흐름이 끊기지 않는다
- 추상적인 화학 개념을 **시각적·동적으로** 즉시 확인할 수 있다
- 별도 앱 설치 없이 **웹 브라우저(iPad Safari)** 만으로 동작한다
- 교사의 **판서 습관을 바꾸지 않고** 기존 수업 방식에 자연스럽게 통합된다
